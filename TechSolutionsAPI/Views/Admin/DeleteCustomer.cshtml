@model TechSolutionsAPI.Models.CustomerViewModel

@{
    ViewData["Title"] = "Delete Customer";
}

<h1>Delete Customer</h1>

@if (Model != null)
{
    <div class="row text">
        <div class="col-md-8 offset-2">
            <h4> Delete Action Confirmation For @Model.FirstName @Model.LastName</h4>

            <div class="form-group text-center">
                <img style="max-width:350px" id="LogoUrlPreview" />
            </div>

            <div class="form-group">
                <label asp-for="FirstName" class="control-label"></label>
                <input asp-for="FirstName" class="form-control " readonly />
                <span asp-validation-for="FirstName" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="LastName" class="control-label"></label>
                <input asp-for="LastName" class="form-control" readonly />
                <span asp-validation-for="LastName" class="text-danger"></span>
            </div>

            <div class="form-group">
                <label asp-for="Address" class="control-label">Job Title </label>
                <input asp-for="Address" class="form-control" readonly />
                <span asp-validation-for="Address" class="text-danger"></span>
            </div>

            <div class="form-group mt-3">
                <form asp-action="DeleteCustomer">
                    <input type="hidden" asp-for="Id" />
                    <input type="submit" value="Confirm" class="btn btn-danger" style="float:right" />
                </form>
                <a class="btn btn-outline-secondary" asp-controller="Admin" asp-action="ShowCustomer">Back</a>
            </div>
        </div>
    </div>
}
else
{
    <p>The user details could not be found :( </p>
}






